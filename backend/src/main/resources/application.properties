spring.application.name=tuition-backend

# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/smart_tuition_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=mehtaneel410@gmail.com
spring.datasource.password=${DB_PASSWORD:placeholder}

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration
# Note: Use a strong secret key in production, at least 256-bit (32 bytes)
jwt.secret=${JWT_SECRET:placeholder_jwt_secret}
jwt.expiration=86400000

# Server Port
server.port=8080

# Mail settings (Replace with real credentials for production)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mehtaneel410@gmail.com
spring.mail.password=${MAIL_PASSWORD:placeholder}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.jackson.serialization.fail-on-empty-beans=false

# Google Gemini AI Configuration
gemini.api.key=${GEMINI_API_KEY:placeholder}

# Razorpay Configuration
razorpay.api.key=${RAZORPAY_API_KEY:rzp_test_TW5DG1yriRqi0Z}
razorpay.api.secret=${RAZORPAY_API_SECRET:BiP2Jg2O1et3SNTuhKonQZ9c}